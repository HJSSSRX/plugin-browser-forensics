[project]
name = "forhacker-plugin-browser-forensics"
version = "0.1.0"
description = "browser-forensics Cell plugin for ForHacker"
requires-python = ">=3.12"
dependencies = ["forhacker"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
]
